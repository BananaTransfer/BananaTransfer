extends ../layout

block content
  .row.justify-content-center.mb-3
    .col-1
      a.btn.btn-outline-secondary(href="/" style="border-radius:12px;")
        i.bi.bi-arrow-left.align-middle
    .col-10.col-md-6.col-lg-4.text-center
      h2.mt-1 Login
    .col-1

  if error
    .row.justify-content-center.mb-3
      .col-12.col-md-8.col-lg-6
        .alert.alert-danger.text-center= error

  .row.justify-content-center.mb-3
    .col-12.col-md-8.col-lg-6
      form(method="POST" action="/auth/login" class="bg-light p-4 rounded shadow-sm")
        input(type="hidden" name="_csrf" value=csrfToken)
        .mb-3
          label.form-label(for="username") Username
          .input-group
            input.form-control(type="text" name="username" id="username" placeholder="username" value=username required minlength=4 maxlength=32 pattern="^[a-z0-9]*$")
            span.input-group-text @#{domain}
        .mb-3
          label.form-label(for="password") Password
          input.form-control(type="password" name="password" id="password" placeholder="password" required minlength=12 maxlength=256)
        .d-flex.justify-content-between.align-items-center.mb-3
          span No account yet? 
            a.ms-1(href="/auth/register") Register
          button.btn.btn-dark(type="submit" data-testid="login-submit-btn") Continue
      .text-center.text-muted.mt-4
        small
          | Forgot your credentials?
          br
          | Please contact your domain administrator

  script(type="module").
    import { setupLoginForm } from '/js/authentication.js';
    setupLoginForm();