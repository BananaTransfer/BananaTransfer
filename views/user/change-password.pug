extends ../layout

block content
  .row.justify-content-center.mb-3
    .col-1
      a.btn.btn-outline-secondary(href="/user" style="border-radius:12px;")
        i.bi.bi-arrow-left.align-middle
    .col-10.col-md-6.col-lg-4.text-center
      h2.mt-1 Change password
    .col-1

  if error
    .row.justify-content-center.mb-3
      .col-12.col-md-8.col-lg-6
        .alert.alert-danger.text-center= error

  .row.justify-content-center.mb-3
    .col-12.col-md-8.col-lg-6
      form(method="POST" action="/user/change-password" class="bg-light p-4 rounded shadow-sm")
        input(type="hidden" name="_csrf" value=csrfToken)
        .mb-3
          label.form-label(for="currentPassword") Current password
          input.form-control(type="password" name="currentPassword" id="currentPassword" placeholder="current password" required)
        .mb-3
          label.form-label(for="password") New password
          input.form-control(type="password" name="password" id="password" placeholder="new password" required)
        .mb-3
          label.form-label(for="confirmPassword") Confirm new password
          input.form-control(type="password" name="confirmPassword" id="confirmPassword" placeholder="password confirmation" required)
        .d-flex.justify-content-between.align-items-center.mb-3
          a.ms-1(href="/user") Back to Settings
          button.btn.btn-dark(type="submit") Change Password

  script(type="module").
    import { setupChangePasswordForm } from '/js/authentication.js';
    setupChangePasswordForm();
